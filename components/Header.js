import Head from 'next/head'

const Header = () => {
  return (
    <div>
      <Head>
        <title>Revent</title>
      </Head>

      <h1 /*className={styles.title}*/>Welcome to Revent!</h1>
    </div>
  )
}

export default Header
